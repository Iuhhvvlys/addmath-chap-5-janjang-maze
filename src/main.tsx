import React from 'react'
import ReactDOM from 'react-dom/client'
import SiriMazeGame from './SiriMazeGame'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <SiriMazeGame />
  </React.StrictMode>,
)